Copyright (c) 2025 Ronan MERIEN

This is a IMP "Interpreteur pour Machine à Pile" (stack machine interpreter in english) in Java for studies purposes.

gram_imp.dat is the grammar of the language use by gbf program (grammar compiler) to compile into tab_imp.dat (binary version) use by the IMP program via the Loader class.

Use the main class to run files from  prgs/*.obj 
